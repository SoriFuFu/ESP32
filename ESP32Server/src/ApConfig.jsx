// ApConfig.jsx
import React from 'react';

const ApConfig = ({ apConfig, handleApEnabledChange, handleApEnabled }) => {
    // Aquí va el contenido del componente ApConfig
    return (
        <div>
            <h2>Configuración AP</h2>
            {/* Contenido del componente */}
        </div>
    );
}

export default ApConfig;
